<div class="container">
<div class="navbar">
<h1>List of restaurants</h1>
</div>

<div class="list">
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %> |</li>
  <% end %>
  </ul>
  <li>
  </div>
  <%= link_to "Create a new restaurant", new_restaurant_path(@restaurant), class: "btn btn-primary"%>
  </li>
</div>
